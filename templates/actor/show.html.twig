{% extends 'index.html.twig' %}

{% block title %}Hello ActorController!
{% endblock %}

{% block body %}
	<h1>{{ actor.name }}</h1>

	{% for program in actor.programs %}
		<li>
			<a href="{{ path("program_show", { slug: program.slug }) }}">{{ program.title }}</a>
		</li>
	{% else %}
		<li>Aucun programme pour cet acteur</li>
	{% endfor %}

{% endblock %}
