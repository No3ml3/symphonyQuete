{% extends 'base.html.twig' %}

{% block title %}Season
{% endblock %}

{% block body %}
	<main>
		<h1>Season</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ season.id }}</td>
				</tr>
				<tr>
					<th>Number</th>
					<td>{{ season.number }}</td>
				</tr>
				<tr>
					<th>Year</th>
					<td>{{ season.year }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ season.description }}</td>
				</tr>
			</tbody>
		</table>
		<ul>
			{% for episode in episodes %}
				<li>{{ episode.number ~ ' ' ~ episode.title ~ ' ' ~ episode.synopsis  }}</li>
			{% else %}
				Aucune episode trouvée.
			{% endfor %}
		</ul>

		<a href="{{ path("program_show", { id: program.id }) }}">back to list</a>

	</main>
{% endblock %}
